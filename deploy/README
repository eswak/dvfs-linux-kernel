At first it is required to reserver nodes in grid5000. Conecet to one sites of grid5000.

1) ssh amkoyan@access.grid500.fr
	# change amkoyan to your username
2) ssh nancy 
	# go to nancy site or other

3) screen
	# get sreen for uninteraptable ssh session

4)oarsub -p "wattmeter='YES'" -I -l cluster=1/nodes=2,walltime=4:0:0 -t deploy
	# have look to resource property

5) kadeploy3 -a ubuntu16.04-x64-min.env  -f $OAR_FILE_NODES -k
	# "ubuntu16.04-x64-min.env" file in src/deploy
6) copy hostnames to src/deploy/cluster.txt

7)  
	7.1)
		export PATH=$PATH:/home/$(USER)/.local/bin/
		# befor starting export local python bin path
	7.2) 
		bash deploy/main.sh
		# start deployment cluster
8) 
	8.1)
		bash postdeploy-smarth-governor.sh
		# After sucsessfull deployment run post deploy manualy on each node to change kernel to 4.10
	8.2)
		screen -d -m sh -c '/tmp/NPB/bin/sar.sh'
		# sysstat script removes afters restart, so ensure that is runing 
9) bash buildSmartGovernor.sh
	# run smart governor
	# make sure you are using right kernel folder, deployment script use "/lib/modules/4.10.0-20-generic/build"

10) mkdir data/;  python AutoExecution/main.py --nodes deploy/cluster.txt -s lyon -o data/
	#run experiment from Measurment/AutoExecution module